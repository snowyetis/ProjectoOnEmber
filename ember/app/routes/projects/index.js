import Ember from 'ember';

export default Ember.Route.extend({
  // model: function(params) {
  //   return this.store.query('project', { param: params.user_id });
  //   // return Ember.RSVP.hash({
  //   //   projects: this.store.findAll('project'),
  //   //   project_details: this.store.findAll('project_detail')
  //   // });
  //   // const project = this,modelFor('project');
  //   // return this.store.query('project_detail', { param: project });
  // },
  //
  // setupController(controller,model) {
  //   controller.set('projects', model.projects);
  //     // controller.set('projects', model);
  //     // this.store.findAll('project_detail').then(function(project_details) {
  //     //   controller.set('project_details', project_details)
  //     // });
  // }
});
